name: Resilience Suite

on:
  workflow_dispatch:

jobs:
  resilience:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Wire this job to the Toxiproxy-backed resilience test profile."
